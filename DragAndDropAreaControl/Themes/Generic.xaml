<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DragAndDropAreaControl">
    <Style TargetType="{x:Type local:DragAndDropArea}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DragAndDropArea}">

                    <!--  TODO:ファイル／フォルダをドラッグオーバーした際に、オーバーレイが表示されるようにしたい  -->
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="16"
                                FontWeight="Bold"
                                Text="TODO HeaderTextプロパティをバインドする" />

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1" />
                                    <ColumnDefinition Width="1" />
                                </Grid.ColumnDefinitions>

                                <!--  ファイル選択ダイアログ  -->
                                <IconButton Grid.Column="0" />

                                <!--  フォルダ選択ダイアログ  -->
                                <IconButton Grid.Column="1" />
                            </Grid>


                            <!--  ドロップされたファイル／フォルダを表示する  -->
                            <!--  TODO:　複数ファイルドロップに対応するためにアイテムコントロールにしたほうがいいかも  -->
                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Text="TODO ここにドロップされたファイルのパスまたはファイル名を表示する" />

                                <!--  削除ボタン  -->
                                <IconButton Grid.Column="1" />
                            </Grid>

                            <TextBlock
                                Grid.Row="3"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="12"
                                Text="TODO ErrorMessageプロパティをバインドする" />

                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
